name: SPARCats
key: simcore/services/comp/sparcats
type: computational
integration-version: 1.0.0
version: 0.1.0
description: Generate sugmented time series physiological data based on data from the SPARC Data Portal.
contact: oath399@aucklanduni.ac.nz
thumbnail: https://github.com/ITISFoundation/osparc-assets/blob/cb43207b6be2f4311c93cd963538d5718b41a023/assets/default-thumbnail-cookiecutter-osparc-service.png?raw=true
authors:
  - name: Omkar Athavale
    email: oath399@aucklanduni.ac.nz
    affiliation: University of Auckland
inputs:
  exp_approach:
    displayOrder: 1
    label: Experimental approach
    description: How was the data measured, not all are available
    type: ref_contentSchema
    contentSchema:
      title: Experimental approach
      default: electrophysiology
      type: string    # allowed input / output types are "string", "number", "bool", "data:*/*"
      enum: [electrophysiology, physiology]
  species:
    displayOrder: 2
    label: input_2_label
    description: The input 2 description
    type: string
    # allowed input / output types are "string", "number", "bool", "data:*/*"
    defaultValue: some_value(optional)
    fileToKeyMap:
      somefilename.ext: input_2
  sex:
    displayOrder: 3
    label: input_3_label
    description: The input 3 description
    type: string
    # allowed input / output types are "string", "number", "bool", "data:*/*"
    defaultValue: some_value(optional)
    fileToKeyMap:
      somefilename.ext: input_3
  input_4:
    displayOrder: 4
    label: input_4_label
    description: The input 4 description
    type: string
    # allowed input / output types are "string", "number", "bool", "data:*/*"
    defaultValue: some_value(optional)
    fileToKeyMap:
      somefilename.ext: input_4
  input_5:
    displayOrder: 5
    label: input_5_label
    description: The input 5 description
    type: string
    # allowed input / output types are "string", "number", "bool", "data:*/*"
    defaultValue: some_value(optional)
    fileToKeyMap:
      somefilename.ext: input_5
  input_6:
    displayOrder: 6
    label: input_6_label
    description: The input 6 description
    type: string
    # allowed input / output types are "string", "number", "bool", "data:*/*"
    defaultValue: some_value(optional)
    fileToKeyMap:
      somefilename.ext: input_6
  input_7:
    displayOrder: 7
    label: input_7_label
    description: The input 7 description
    type: string
    # allowed input / output types are "string", "number", "bool", "data:*/*"
    defaultValue: some_value(optional)
    fileToKeyMap:
      somefilename.ext: input_7
  input_8:
    displayOrder: 8
    label: input_8_label
    description: The input 8 description
    type: string
    # allowed input / output types are "string", "number", "bool", "data:*/*"
    defaultValue: some_value(optional)
    fileToKeyMap:
      somefilename.ext: input_8
  
outputs:
  output_1:
    displayOrder: 1
    label: Exact results
    description: Exact matches to search
    type: data:*/*
    fileToKeyMap:
      output_exact.json: output_1
  output_2:
    displayOrder: 1
    label: Relevant results
    description: Inexact matches to search
    type: data:*/*
    fileToKeyMap:
      output_relevant.json: output_2